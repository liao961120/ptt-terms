---
title: 批踢踢常用詞彙
date: "`r format(Sys.Date(), '%B %d, %Y')`"
output:
  bookdown::html_document2:
    css: style.css
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
	echo = FALSE,
	fig.dim = c(7, 4),
	message = FALSE,
	warning = FALSE,
	comment = "",
	dev = "svg",
	dpi = 350,
	out.width = "100%"
)
```

```{r}
library(DT)
dat <- readRDS("dict.rds")
colnames(dat) <- c("PTT 用語", "HTML Tag")
```


此網頁提供**[批踢踢](https://term.ptt.cc/)常見用語**的查找與[下載]()

這裡自 [PTT 鄉民百科](http://zh.pttpedia.wikia.com/wiki/) 擷取。


<br>

```{r}
datatable(dat, rownames = FALSE, filter = "bottom",
          options = list(
              pageLength = 15,
              #dom = 'tip',
              columnDefs = list(list(className = 'dt-center', 
                                     targets="_all")
                                )
              )
          )
```

